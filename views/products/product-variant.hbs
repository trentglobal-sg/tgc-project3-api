{{#extends 'base'}}
{{#block 'content'}}

<h1>Product Variants</h1>
<h2>{{product.product}}</h2>
<div>
    <img src="{{variant.variant_image_url}}" style="width:300px" />
</div>
{{!-- <div>
    <h3>Description: </h4>
    <p>{{product.description}}</p>
</div>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Created</th>
            <th>Brand</th>
            <th>Category</th>
            <th>Gender</th>
            <th>Activity</th>
            <th>Blend</th>
            <th>Micron</th>
            <th>Fit</th>
            <th>Cost</th>
        </tr>
        <tr>
            <td>{{product.id}}</td>
            <td>{{moment product.created_date format="YYYY-MM-DD"}}</td>
            <td>{{product.brand.brand}}</td>
            <td>{{product.category.category}}</td>
            <td>{{product.gender.gender}}</td>
            <td>{{product.activity.activity}}</td>
            <td>{{product.blend.blend}}</td>
            <td>{{product.micron.micron}}</td>
            <td>{{product.fit.fit}}</td>
            <td>${{divide product.cost 100}}</td>
        </tr>
    </thead>
</table> --}}

<div>
    <h3>Color Variant:</h4>
</div>
<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Color Name</th>
            <th>Color Code</th>
            {{!-- <th>Image</th> --}}
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>{{variant.id}}</td>
            <td>{{variant.color_name}}</td>
            <td>{{variant.color_code}}</td>
            {{!-- <td><img src="{{variant.variant_thumbnail_url}}" /></td> --}}
        </tr>
    </tbody>
</table>

<div><h3>Product Variants</h3></div>
<a href="/products/{{productId}}/variants/{{variantId}}/add-product-variant/" class="btn btn-sm btn-primary">Add New Product Variant</a>
<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Size</th>
            <th>Stock</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        {{#each productVariants}}
        <tr>
            <td>{{this.id}}</td>
            <td>{{this.size.size}}</td>
            <td>{{this.stock}}</td>
            <td><a href="/products/{{../productId}}/variants/{{../variantId}}/update-product-variant/{{this.id}}" class="btn btn-sm btn-primary">Update</a></td>
        </tr>
        {{/each}}
    </tbody>
</table>

<a href="/products/{{productId}}" class="btn btn-sm btn-primary mt-6">Back to Product</a>

{{/block}}
{{/extends}}